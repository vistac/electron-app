{
  "name": "electron-gui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/electron-gui/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx-electron:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/electron-gui",
        "main": "apps/electron-gui/src/main.ts",
        "tsConfig": "apps/electron-gui/tsconfig.app.json",
        "assets": ["apps/electron-gui/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/electron-gui/src/environments/environment.ts",
              "with": "apps/electron-gui/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "nx-electron:execute",
      "options": {
        "buildTarget": "electron-gui:build"
      }
    },
    "package": {
      "executor": "nx-electron:package",
      "options": {
        "name": "electron-gui",
        "frontendProject": "frontend-gui",
        "sourcePath": "dist/apps",
        "outputPath": "dist/packages",
        "prepackageOnly": true
      }
    },
    "make": {
      "executor": "nx-electron:make",
      "options": {
        "name": "electron-gui",
        "frontendProject": "frontend-gui",
        "sourcePath": "dist/apps",
        "outputPath": "dist/executables"
      }
    },
    "lint": {
      "command": "eslint apps/electron-gui/**/*.ts"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/electron-gui/jest.config.ts"
      }
    }
  }
}
